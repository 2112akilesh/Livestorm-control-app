import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.controller.app',
  appName: 'controller',
  webDir: 'src',
  bundledWebRuntime: false
};

export default config;
